### Lokale Secrets (nicht committen)

- **Datei**: `secrets/replicate.local.json`
- **Inhalt**: Dein Replicate Token

Die Datei ist per `.gitignore` ausgeschlossen.

Beispiel:

```json
{
  "REPLICATE_API_TOKEN": "r8_...DEIN_TOKEN..."
}
```


